# Filled by `pnpm exec convex dev`; do not put provider secrets in this file.
CONVEX_DEPLOYMENT=
VITE_CONVEX_URL=
VITE_CONVEX_SITE_URL=

# Alpha-only fallback. Set to true in Convex when no transactional email provider is configured.
AUTH_ALLOW_UNVERIFIED_EMAIL=
